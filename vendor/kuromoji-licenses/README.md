# Bundled reading engine notices

The Learning Hub includes the browser build and dictionary files distributed with `kuromoji` 0.1.2. The JavaScript library is licensed under Apache License 2.0; the bundled IPADIC data has the notices reproduced in `NOTICE.md`.

These assets are used locally in the browser to add display-only readings to Japanese text that does not already include explicit ruby markup.
